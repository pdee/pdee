language: python

before_install:
  sudo apt-get update -qq &&
  sudo pip install ipython3 &&
  sudo pip install ipython2.7 &&
  sudo apt-get install -qq emacs24 emacs24-el emacs24-common-non-dfsg ipython jython;

env:
  EMACS=emacs24

script:
  ./run-travis-ci.sh